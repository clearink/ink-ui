import { useBreakpoint } from '@mink-ui/core/_shared/hooks'
import Col from '@mink-ui/core/col'
import Row from '@mink-ui/core/row'

// CompoundedGrid
const Grid = { Col, Row, useBreakpoint }

/**
 * |---------------------------------------------------------|
 * |---------------------------------------------------------|
 * |                    export definition                    |
 * |---------------------------------------------------------|
 * |---------------------------------------------------------|
 */

export { Grid }
export default Grid
